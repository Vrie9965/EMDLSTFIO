# Comment Subs Folder
This directory stores frame-based subtitle images used for post comments.

- Filename format: `frame_<number>.jpg`
- Example: `frame_123.jpg`
- The `<number>` should match the frame number being posted from `frames/`.
